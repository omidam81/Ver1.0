@using (Html.BeginFormAntiForgeryPost(Url.Action("Register", "Account", new {ReturnUrl = Request.QueryString["ReturnUrl"]})))
{
    <fieldset>
        <legend>@T("Account Information")</legend>
        <div>
            <label for="email">@T("Email:")</label>
            @Html.TextBox("email")
            @Html.ValidationMessage("email")
        </div>
        <div>
            <label for="password">@T("Password:")</label>
            @Html.Password("password")
            @Html.ValidationMessage("password")
        </div>
        <div>
            <label for="confirmPassword">@T("Confirm password:")</label>
            @Html.Password("confirmPassword")
            @Html.ValidationMessage("confirmPassword")
        </div>
        <div>
            <button class="primaryAction" type="submit">@T("Register")</button>
        </div>
    </fieldset>
}


@*
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div> 
    </div>
</body>
</html>
*@